# Applications

Deployable processes live here. Applications are composition roots: they configure dependencies and expose domain capabilities without owning reusable business rules.

- `api`: FastAPI HTTP and server-sent-event interface.
- `worker`: background workflow workers and scheduler entry points.
- `dashboard`: Next.js user interface.
