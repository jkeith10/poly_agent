# `oracle_api`

Python source root for the API application. Future modules in this namespace may depend on package application interfaces and infrastructure adapters, but domain packages must never depend on this application.
