# API application

Composition root for FastAPI, authentication, authorization, REST resources, streaming updates, health checks, and dependency wiring. Business logic belongs in packages under `packages/`.
