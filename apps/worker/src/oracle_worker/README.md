# `oracle_worker`

Python source root for worker and scheduler entry points. Celery-specific concerns remain at this boundary so domain and application services are portable to another workflow engine.
