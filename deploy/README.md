# Deployment

Deployment definitions are separated by environment while sharing immutable application images and validated configuration contracts.

- `compose`: local and CI orchestration.
- `railway`: early hosted-environment definitions.
- `aws`: production cloud infrastructure.
