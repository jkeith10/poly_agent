# Packages

Reusable Python packages follow inward dependency direction: presentation and infrastructure depend on application and domain layers, never the reverse. Cross-package communication uses typed interfaces and immutable domain events.
