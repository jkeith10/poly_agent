# Database

PostgreSQL infrastructure including SQLAlchemy mappings, Alembic migrations, repository implementations, transactions, and the transactional outbox. Domain entities and repository interfaces remain owned by their respective domains.
